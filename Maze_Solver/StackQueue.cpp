#include "StackQueue.h"

using namespace std;

/*Maze maze("input_maze.txt");
    cout << "ape" << endl;

    maze.print_state();
    maze.move_right();
    maze.move_forward();
    maze.move_left();
    maze.move_forward();
    maze.move_forward();
    maze.move_right();
    maze.move_forward();
    maze.move_forward();
    maze.move_forward();
    maze.move_left();
    maze.move_forward();
    if(maze.is_solved())
        cout << "finito" << endl;
    maze.move_left();
    maze.print_state();*/

/*    StackQueue<int> obj(4);

    int front = obj.get_front();
    int rear = obj.get_rear();

    cout << "front: " << front << " rear: " << rear << endl;
    obj.push_rear(1);
    obj.push_rear(2);
    obj.push_rear(3);
    obj.push_rear(4);

    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.pop_front();
    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.pop_rear();
    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.pop_front();
    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.pop_front();
    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.push_front(55);
    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.push_front(66);
    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.push_front(33);
    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.push_front(1);
    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.push_front(22);
    front = obj.get_front();
    rear = obj.get_rear();
    cout << "front: " << front << " rear: " << rear << endl;

    obj.print_elements();*/
